const chai = require('chai');
const assert = require('chai').assert;
const Cell = require( '../cell.js');

describe('cell', () => {
    it('Instantiate', () => {
        let cell = new C
      
    });

});